<style type="text/css" media="screen">
  .jumbotron{
    background: <%= @category.color %> !important;
  }
</style>
<div class="jumbotron">
  <div class="">
    <% if !@category.user.nil? %>
      <p class="">Creado Por <strong> <%= @category.user.email %> </strong></p>
    <%end%>
  </div>
  <div class="">
    <h1><strong><%= @category.name %></strong></h1>
  </div>
  <div class="">
    <h3><%= @category.color %></h3>
  </div>
  <div class="">
    <%= link_to 'Edit', edit_category_path(@category), class: "btn btn-warning" %>
    <%= link_to 'Back', categories_path, class: "btn btn-info" %>
    <%= link_to 'Eliminar', { :action => :destroy, :id => @category.id }, :method => :delete, :data => { :confirm => 'Estás seguro? de Eliminar Esta Categoria #{@category.name} '} , class: "btn btn-danger"%>
  </div>
</div>
